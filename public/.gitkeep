# Next.js `public/` directory — static assets served from the site root.
# Currently empty (the favicon lives at app/icon.png via Next's file-based
# convention). Drop og-image.png, robots.txt, sitemap.xml, etc. here.
