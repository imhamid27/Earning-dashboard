yfinance>=0.2.40
# RSS feed parser — used by fetch_related_coverage.py to consume Google News
# RSS without needing API keys. Handles RSS 2.0/Atom + date parsing edge cases.
feedparser>=6.0.0
supabase>=2.5.0
python-dotenv>=1.0.1
pandas>=2.2.0
# NSE scrapers need Chrome TLS fingerprint impersonation — plain `requests`
# is blocked at the edge.
curl_cffi>=0.7.0
# Same-day result-PDF parsing (bse_pdf_results.py). Screener's quarterly
# index lags the BSE filing by 2-12 hours; the PDF is live the moment the
# announcement hits. Pure-Python — no Java runtime needed.
pdfplumber>=0.11.0
# OCR fallback for scanned-image result PDFs (Navkar Corp, ICICI AMC,
# Aqylon, etc.). pymupdf rasterises pages → pytesseract OCRs. The native
# tesseract binary is installed separately by CI (apt-get install
# tesseract-ocr in the GitHub Actions workflow). Fitz (pymupdf) is also
# a better text-extraction engine than pdfplumber for some filings.
pymupdf>=1.24.0
pytesseract>=0.3.10
Pillow>=10.0.0
# Claude API — generates 2-sentence investor briefs for the "In Context" card.
anthropic>=0.20.0
